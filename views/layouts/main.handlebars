<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Expense Tracker</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-gray-100">
  <nav class="bg-gray-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <a class="text-white font-bold text-xl" href="/">Expense Tracker</a>
        <div class="flex space-x-4">
          <a class="text-gray-300 hover:text-white px-3 py-2 rounded-md" href="/">Home</a>
          {{#if logged_in}}
          <a class="text-gray-300 hover:text-white px-3 py-2 rounded-md" href="/dashboard">Dashboard</a>
          <button class="text-gray-300 hover:text-white px-3 py-2 rounded-md" id="logout">Logout</button>
          {{else}}
          <a class="text-gray-300 hover:text-white px-3 py-2 rounded-md" href="/login">Login</a>
          {{/if}}
        </div>
      </div>
    </div>
  </nav>

  <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
    {{{ body }}}
  </main>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>